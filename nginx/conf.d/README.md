# nginx/conf.d/

This directory is managed by `setup.sh`.

Running `./setup.sh` will generate `mdcompliance.conf` here with your
domain name and SSL configuration already filled in.

Do not manually create `mdcompliance.conf` — run `setup.sh` instead.
